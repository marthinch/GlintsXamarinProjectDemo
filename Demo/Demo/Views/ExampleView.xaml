<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Demo.Views.ExampleView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:Demo.ViewModels">
    <!--<ContentPage.BindingContext>
        <viewmodels:ExampleViewModel />
    </ContentPage.BindingContext>-->
    <ContentPage.Content>
        <StackLayout
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="400">

            <Label FontSize="Large" Text="Deskripsi" />
            <Entry
                FontSize="Large"
                Placeholder="Ini awal deskripsi"
                Text="{Binding Description}" />

            <Label FontSize="Large" Text="Hasil:" />
            <Label FontSize="Large" Text="{Binding Description}" />

            <Button Command="{Binding BukaHalamanCommand}" Text="Halaman Berikutnya" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>